const router = require('express').Router(); const asyncHandler = require('../utils/asyncHandler'); const c = require('../controllers/cartController'); const { requireAuth } = require('../middlewares/auth'); router.use(requireAuth); router.get('/', asyncHandler(c.show)); router.post('/add', asyncHandler(c.add)); router.post('/:itemId', asyncHandler(c.update)); router.post('/:itemId/remove', asyncHandler(c.remove)); module.exports = router;
