const router = require('express').Router(); const asyncHandler = require('../utils/asyncHandler'); const c = require('../controllers/accountController'); const orders = require('../controllers/orderController'); const { requireAuth } = require('../middlewares/auth'); router.use(requireAuth); router.get('/', asyncHandler(c.dashboard)); router.get('/profile', asyncHandler(c.profile)); router.post('/profile', asyncHandler(c.updateProfile)); router.post('/password', asyncHandler(c.changePassword)); router.post('/logout-all', asyncHandler(c.logoutAll)); router.get('/purchases', asyncHandler(orders.purchases)); module.exports = router;
