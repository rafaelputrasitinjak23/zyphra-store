const router = require('express').Router(); const asyncHandler = require('../utils/asyncHandler'); const c = require('../controllers/orderController'); const { requireAuth } = require('../middlewares/auth'); const { cancelLimiter } = require('../middlewares/rateLimits'); router.use(requireAuth); router.get('/', asyncHandler(c.list)); router.post('/:orderNumber/cancel', cancelLimiter, asyncHandler(c.cancel)); router.get('/:orderNumber/invoice', asyncHandler(c.invoice)); router.get('/:orderNumber', asyncHandler(c.detail)); module.exports = router;
